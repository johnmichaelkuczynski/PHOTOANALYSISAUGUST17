services:
  - type: web
    name: ai-personality-analysis
    env: node
    buildCommand: npm run build && mkdir -p server/public && cp -r dist/public/* server/public/ && cp dist/index.js src/dist/index.js
    startCommand: node src/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000